# tone-deaf

Terminal-based musical ear training app.

## Dependencies

- **Python 3** (standard library only, no pip packages)
- **paplay** (PulseAudio) — WAV playback on Linux
- **fluidsynth** (optional) — higher-quality audio; a SoundFont is downloaded automatically on first run

## Usage

```
python ear_trainer.py
```
